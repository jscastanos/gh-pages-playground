name: End Workflow
on:
  workflow_run:
    workflows:
      - 'Start Workflow'
    types:
      - completed
  workflow_dispatch:


jobs:
  end:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Print End
        run: echo end


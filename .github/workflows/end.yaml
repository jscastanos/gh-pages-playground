name: End Workflow
on:
  workflow_run:
    workflows:
      - 'Start Workflow'
    types:
      - completed
  workflow_dispatch:


jobs:
  end:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Do something busy
        uses: juliangruber/sleep-action@v1
        with:
          time: 10m

      - name: Print End
        run: echo end


